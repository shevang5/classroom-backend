// Supported from v16.8.3 till 20.2.2

var serverWrapper = require("./../core-modules");
var moduleName = "Hapi";

var moduleInfo = {
    functions: [
        {
            functionName: "server",
            component: moduleName,
            wrapper: wrapServer
        }
    ]
};

function wrapServer(actual) {
    return serverWrapper.serverWrap(actual);
}

module.exports = moduleInfo;
